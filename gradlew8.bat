@echo off
rem ============================================================
rem  DEPRECATED — 本文件属于旧的 ForgeGradle 1.2 / JDK 8 环境，
rem  项目已迁移到 GTNH 工具链（JDK 17+，推荐 JDK 25）。
rem  请直接使用 gradlew.bat 或 build.bat / runClient.bat。
rem  旧的构建脚本备份在 legacy-forgegradle\ 目录。
rem ============================================================
echo.
echo [已废弃] 本项目已迁移到 GTNH 工具链，不再需要 JDK 8。
echo 请改用:  gradlew.bat ^<task^>     例如 gradlew.bat build
echo 或直接双击:  build.bat / runClient.bat / setup.bat
echo.
pause
exit /b 1
